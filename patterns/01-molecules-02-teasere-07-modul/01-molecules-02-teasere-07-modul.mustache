{{# patternInfo }}
    <div class="module_box_style mbl">
        <h2 class="mtn">Modul</h2>
        <p>En <i>pattern</i> som blir brukt som etjeneste 100% på leirskole, skole, barnehage, temaside, orgenhet, byrådsavdeling og bydelssiden. I tillegg brukes den til å vise tall og fakta på barnehagesidemal, om oss på orgenhetssidemal, og tall og fakta-helside. <a href="{{ link.pages-etjeneste-eksempler }}">Se modul-eksempler her</a>.</p>
        <p>Spesiell egenskap: Innholdsfeltet kan inneholde rik tekst inkl. lenker.</p>
    </div>
{{/ patternInfo }}

<div class="module_container {{ styleModifier }}">
    {{# bilde }}
    <div class="module_image_container">
        {{# url }}
        <a href="{{ url }}" title="{{ modulTittel }}" class="module_image_link">
        {{/ url }}
            <img src="{{ imgLandscape.src }}" alt="{{ imgLandscape.alt }}" class="module_image" />
        {{# url }}
        </a>
        {{/ url }}
        {{# byrad }}
        <p class="councilor">
            Se egen side for:<br>
            <a href="{{ url }}" title="Byrådet sin side">
                <span class="read_more_link">{{ tittel }}</span>
            </a>
        </p>
        {{/ byrad }}
    </div>
    {{/ bilde }}
    <div class="module_content_container">
        {{# modulTittel }}
            <h2 class="module_content_title">
                {{# url }}
                <a href="{{ url }}">
                <span class="read_more_link read_more_link--22">
                {{/ url }}
                    {{ modulTittel }}
                {{# url }}
                </span>
                </a>
                {{/ url }}
            </h2>
        {{/ modulTittel }}
        <div class="module_content_excerpt">
            {{{ htmlIngress }}}
            {{# lesMerLink }}
            <p>
                <a href="{{ url }}"><span class="read_more_link">Les mer om oss</span></a>
            </p>
            {{/ lesMerLink }}
        </div>
    </div>
</div>